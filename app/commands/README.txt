The commands, ie the classes in thi directory that implement the interface_command
are scanned and put into an array automatically.

// TODO: a `move_creator` command (ie move creatorship to someone else)
// TODO: currently when args of commands are out of bounds (say negative percentage) no error description is returned
// TODO: integrate request, offer and merge commands
// TODO: a `change_merge_policy` command